---
"@ludovicm67/simple-whiteboard": minor
---

Add stacking-order controls: move a selected item forward/backward or to the front/back, propagated through a new `reorder` event (`moveItemForward`/`moveItemBackward`/`bringItemToFront`/`sendItemToBack`/`moveItemToIndex`/`applyItemsOrder` API)
